//Crie a mesma função do exercício 1, mas com 2 argumentos: `firstName` e `lastName`.
//Dentro da função, exiba o nome completo.

function nomeCompleto(firstName, lastName) {
    return `${firstName} ${lastName}`

}

console.log(nomeCompleto('Alberto', 'Barbosa'))