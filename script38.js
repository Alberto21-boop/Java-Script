//Defina uma variável `number` com valor 1000.
//Crie um loop `for` que se repita 10 vezes.
//A cada iteração do loop, incremente o valor de `number` em 100.
//Mostre o valor de `number` no console.

let number = 1000;

for (let i = 0; i <10; i++) {
    number+= 100;
    console.log(number)
}